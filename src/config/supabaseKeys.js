// Chaves do seu projeto Supabase (Painel > Settings > API).
//
// Cole a "Project URL" e a "anon public" key aqui. A anon key é PÚBLICA e
// segura de versionar: o acesso aos dados é protegido por Row Level Security
// no Supabase (cada usuário só enxerga a própria linha).
//
// Enquanto ficarem vazias, o app roda em modo local (sem login), como antes.
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
